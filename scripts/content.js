const content = {

	pictureContent: "",
	homeContent: "",
	videoContent: "",


	switchPage = (page) => {
		const wrapper = document.getElementById("outer-wrapper");
		
		//Remove any content from wrapper if it exists
		if(page == "homepage") {
			//add this.homeContent to wrapper
		} else if(page == "videopage") {
			//add this.videoContent
		} else if(page == "pictureContent") {
			//add this.pictureContent 
	}





}
